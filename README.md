# 关于用户中心项目开发

> 引言：这是我的第一个从开发到部署最后上线的项目，感谢b站up主：鱼皮的教程

## 项目简介

功能：用户管理系统

目的：了解做项目的思路，接触一些企业级的开发技术

目标：能做出管理系统

### 所用到的技术

前端：三件套+React+组件库 Ant Design+Umi+Ant Design Pro（现成的管理系统）

后端：Java（编程语言）

spring（依赖注入框架，帮助管理Java对象，集成一些其他内容）

springmvc（web框架，提供接口访问、restful接口等能力）

mybatis（Java操作数据库的框架，持久层框架，对jdbc的封装）

mybatis-plus（对mybatis的增强吗，不用写sql也能实现增删改查）

springboot（快速启动/快速集成项目。不用自己管理spring配置，不用自己整合各种框架）

mysql

Junit（单元测试库）

部署：服务器/容器（平台）

## 企业项目流程

> 其中设计、写代码、测试、优化是个循环

1. 需求分析
2. 设计（概要设计、详细设计）
3. 技术选型
4. 初始化/引入需要的技术
5. 写Demo
6. 写代码（实现业务逻辑）
7. 测试（单元测试）
8. 代码提交/代码评审
9. 部署
10. 发布

### 需求分析

1. 登录/注册
2. 用户管理（仅管理员可见）对用户的查询或者修改
3. 用户校验（仅星球/vip用户）

### 设计

#### 概要设计（首要）

1、初始化设计
    1、前端初始化
              1、初始化项目
              2、引入一些组件
              3、瘦身
    1、后端初始化
              1、准备环境
              2、引入框架

2、数据库设计

3、登录/注册实现
    1、后端
              1、瘦身
              2、实现数据库基本操作
                     1、模型对象：根据数据库字段自动生成
              3、写登录逻辑
    2、前端

4、用户管理

#### 细节设计（写完Demo后执行）

##### 注册设计

逻辑：

1、用户在前端输入账户和密码、以及校验码（todo）

2、校验用户的账户、密码、校验密码是否符合要求

3、对密码进行加密（密码不要以明文存储到数据库中）

4、向数据库插入用户数据

##### 登录设计

接口设计：

接受参数：用户账户、密码

请求类型：POST

请求体：JSON格式

> 请求参数很长时不建议用GET

返回值：用户信息（脱敏）

> [!NOTE]
>
> 脱敏：新生成一个对象，只设置允许返回给前端的值

逻辑

1、校验用户账户和密码是否违法

2、校验密码是否输入正确，要和数据库中的密文密码去对比

3、返回用户信息（脱敏，隐藏敏感信息），防止数据库中的字段泄露给前端

4、录用户的登录态（session），将其存到服务器上（用后端SpringBoot框架封装的服务器tomcat去记录）cookie

5、返回脱敏后的用户信息

###### 如何知道是哪个用户登录

1、连接服务器端后，得到一个session 状态（匿名会话），返回给前端

2、登录成功后，得到了登录成功的session，并且给该session设置一些值（比如用户信息），返回给前端一个设置cookie的“命令”

3、前端接收到后端的命令后，设置cookie，保存到浏览器内

4、前端再次请求后端的时候（相同的域名），在请求头中带上cookie去请求

5、后端拿到前端传来的cookie，找到对应的session

6、再从session中取出基于该session存储的变量（用户的登录信息、登录名）

##### 用户管理

查询用户

删除用户

##### 前后端交互

###### 代理

正向代理：替客户端向服务器发送请求

反向代理：替服务器接收请求

##### 用户校验

> 仅适用于用户可信的情况

先让用户自己填：2-5位编号

后台补充对编号的校验：长度校验，唯一性校验

### 技术选型（如上）

### 初始化

#### 前端

AntDesignPro框架安装：[开始使用 - Ant Design Pro](https://pro.ant.design/zh-CN/docs/getting-started/)

根据需求自己瘦身

#### 后端

springboot框架在IDEA自动生成

设计数据库

controller层：不做任何业务处理，接受请求

service层：编辑业务逻辑

mapper层：操作数据

utils：工具类

> 使用mybatisX根据数据库生成domain实体对象、mapper、mapper.xml、service、impl，因此只要写controller、serviceImpl以及相关utils等

### 写Demo进行初步测试

查看配置是否正确能否正确运行

### 写代码

#### 流程

先做设计

代码实现

持续优化（复用代码，提取公共逻辑/常量）

### 测试

测试的包名与业务代码的结构一致

使用generateAllSetter生成测试用例

> [!NOTE]
>
> 如果X生成使用驼峰生成，框架自动开启驼峰命名规则映射，因此要关闭自动转换

### 优化

#### 后端

1、通用返回对象，给对象补充一些信息 
目的：给对象补充一些信息，告诉前端这个请求在业务层面上是成功还是失败 
自定义错误码：返回类支持返回正常和错误

2、封装全局异常处理
     1、定义业务异常类    
              1、相对于java的异常类，支持更多字段      
              2、自定义构造函数，更灵活/快捷的设置字段 
      2、编写全局异常处理器     
           作用：      
               1、捕获代码中的所有的异常，内部消化，集中处理，让前端得到更详细的业务报错/信息      
               2、同时屏蔽掉项目框架本身的异常（不暴露服务器内部状态）
               3、集中记录日志   
                       实现：      1、Srping AOP：在调用方法前后进行额外的处理

3、全局请求日志和登录校验

#### 前端

1、对接后端的返回值，取data

2、全局响应处理 
        1、应用场景：需要对接口的通用响应进行统一处理，比如从response中取出data；或者根据code取集中处理错误，比如用户未登录、没权限之类的     
        2、优势：不用在每个接口请求中都去写相同的逻辑 
        3、实现：参考用的请求封装工具的官方文档，比如umi-request 
如果用的axios，参考axios的文档。创建新的文件，在该文件中配置一个全局请求类，在该文件中配置一个全局请求类。在发送请求时，使用我们自己的定义的全局请求类

### 多环境

定义：同一套项目代码在不同的阶段需要根据实际情况来部署到不同的机器上

目的：

#### 前端

项目的配置

   不同的项目（框架）都有不同的配置文件，umi的配置文件是config

 可以在配置环境后添加对应的环境名称后缀来区分
开发环境：config.dev.ts
生产环境：config.prod.ts
通用环境：config.ts

#### 后端

SpringBoot项目，通过application.yml添加不同的后缀来区分

可以在启动项目时传入环境变量

```bash
java -jar .\\jar包 --spring.profiles.active=prod
```



### 项目部署

Docker部署(主流)结合宝塔以及Docker部署

### 发布

绑定域名

前端项目访问：用户输入网址→域名解析服务器（把网址解析为ip地址/交给其他的域名解析服务）→

nginx接收请求，找到对应的文件，返回文件给前端→前端加载文件到浏览器中（js、css）→渲染页面

后端项目访问：用户输入网址→域名解析服务器→服务器→后端项目（比如8080端口）

nginx反向代理：替服务器接受请求，转发请求